# Private local database
planner.db
*.db
*.sqlite
*.sqlite3

# Secrets / local environments
.env
.venv/

# Python / macOS cache
__pycache__/
*.pyc
.DS_Store

# Local-only backups and temp files
backups/
tmp/

# Session-local tooling and personal (non-app) documents
.claude/
career_goals_context/

# Personal data — never committed (sanitized defaults ship in source instead)
*.local.json
private/
web/public/planner_data.json
web/public/planner_app.zip
web/public/parity_fixture.json
